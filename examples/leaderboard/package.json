{
  "name": "Leaderboard",
  "version": "0.0.1",
  "description": "Leaderboard example using PostgreSQL reactive",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "meteor-pg-live": "../.."
  }
}
